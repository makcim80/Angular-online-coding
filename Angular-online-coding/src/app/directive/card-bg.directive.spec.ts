import { CardBgDirective } from './card-bg.directive';

describe('CardBgDirective', () => {
  it('should create an instance', () => {
    const directive = new CardBgDirective();
    expect(directive).toBeTruthy();
  });
});
